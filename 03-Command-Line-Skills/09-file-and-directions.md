# File and Directions
* `ls` – List files
* `ls -l` – List files with details
* `ls -l /home` – List `/home` with details
* `whoami` – Show username
* `uname` – Show system info
* `uname -n` – Show hostname
* `pwd` – Show current directory
